module hello {

  array [1D] Hello {
    entry Hello(int nVertices, int nEdges, int edges[nEdges * 3], double weights[nEdges]);
    entry void ProcessFragment(int root);
    entry void Receive(map<int, bool> fragment);
    entry void UpdateParent(int child, int parent);
    entry void PromoteRank(int parent);
  };

};
