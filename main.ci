mainmodule main {

  readonly CProxy_Main mainProxy;

  extern module hello;

  mainchare Main {
    entry Main(CkArgMsg* msg);
    entry void done();
    //entry [reductiontarget] void reduce(CkReductionMsg *msg);
    entry [reductiontarget] void reduce(int uniteAmount);
    entry void push(int id);
  };

};
